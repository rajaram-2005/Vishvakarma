{
  "name": "@sutra/orchestration",
  "version": "0.1.0",
  "private": true,
  "description": "Unified AI Studio ONE CORE: capability graph + compatibility engine + universal task graph + state machine + event bus + tracer + model router/fallback/health + matrix + context + validation + cost + approval + search + ensembles + privacy + packages + explain + job queue.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "vishva": "./bin/cli.ts"
  },
  "scripts": {
    "cli": "tsx bin/cli.ts",
    "serve": "tsx server.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@sutra/shared": "*",
    "@sutra/model-adapters": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
