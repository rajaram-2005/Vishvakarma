apiVersion: apps/v1
kind: Deployment
metadata:
  name: aetherion-api
  namespace: aetherion
  labels:
    app.kubernetes.io/name: aetherion-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: aetherion-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: aetherion-api
    spec:
      containers:
        - name: api
          image: aetherion/api:0.1.0
          ports:
            - containerPort: 8000
          env:
            - name: Aetherion_ENV
              value: production
            - name: Aetherion_PRIVACY_MODE
              valueFrom:
                configMapKeyRef:
                  name: aetherion-config
                  key: privacyMode
            - name: OLLAMA_URL
              valueFrom:
                configMapKeyRef:
                  name: aetherion-config
                  key: ollamaUrl
            - name: OPENAI_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: aetherion-secrets
                  key: openaiBaseUrl
                  optional: true
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: aetherion-secrets
                  key: openaiApiKey
                  optional: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: aetherion-api
  namespace: aetherion
spec:
  selector:
    app.kubernetes.io/name: aetherion-api
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: aetherion-config
  namespace: aetherion
data:
  privacyMode: local
  ollamaUrl: http://localhost:11434
