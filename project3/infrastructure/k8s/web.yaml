apiVersion: apps/v1
kind: Deployment
metadata:
  name: sutra-web
  namespace: sutra
  labels:
    app.kubernetes.io/name: sutra-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: sutra-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sutra-web
    spec:
      containers:
        - name: web
          image: sutra/web:0.1.0
          ports:
            - containerPort: 3000
          env:
            - name: SUTRA_API_URL
              value: http://sutra-api:8000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: sutra-web
  namespace: sutra
spec:
  selector:
    app.kubernetes.io/name: sutra-web
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sutra
  namespace: sutra
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: sutra.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sutra-web
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: sutra-api
                port:
                  number: 8000
