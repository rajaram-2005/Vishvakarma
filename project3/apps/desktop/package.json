{
  "name": "@sutra/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Aetherion desktop — Tauri v2 shell around the Aetherion workspace",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "dev:web": "npm --prefix ../web run dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@tauri-apps/plugin-shell": "^2.0.0"
  }
}
